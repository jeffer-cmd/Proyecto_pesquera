<div class="card">
        <div class="card-body">
            <p>{{item.nombre_categoria}}</p>
            {{!-- <p>{{item.categoria}}</p> --}}
            {{!-- <a href="" class="btn btn-primary">Agregar</a> --}}
            <a href="/eliminar/{{item.id_categoria}}" class="btn btn-primary">Eliminar</a>
            <a href="/editar/{{item.id_categoria}}" class="btn btn-primary">Editar</a>
        </div>
    </div>