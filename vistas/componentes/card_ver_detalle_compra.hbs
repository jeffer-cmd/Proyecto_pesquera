<tr>
    <td>{{item.id}}</td>
    <td>{{item.proveedor}}</td>
    <td>{{item.producto}}</td>
    <td>{{item.lote}}</td>
    <td>{{formatoNumero item.cantidad}}</td>
    <td>{{formatoCOP item.precio}}</td>
    <td>{{item.observaciones}}</td>

    

    <td>
        <a href="/gestion_compras/form_editar_compra/{{item.detalleId}}" class="btn btn-warning btn-sm">✏️</a>
        {{!-- <a href="/gestion_compras/eliminar_compra/{{item.id}}" class="btn btn-danger btn-sm">🗑️</a> --}}
    </td>
</tr> 