<tr>
    <td>{{item.nombre}}</td>
    <td>{{item.telefono}}</td>
    <td>{{item.direccion}}</td>
    <td>{{item.email}}</td>
    <td>{{item.createdAt}}</td>
   

    <td class="text-nowrap">
        <div class="d-flex justify-content-center gap-2">
            <a href="/gestion_proveedores/form_editar_proveedor/{{item.id}}" class="btn btn-warning btn-sm">✏️</a>
            <a href="/gestion_proveedores/eliminar_proveedor/{{item.id}}" class="btn btn-danger btn-sm">🗑️</a>

        </div>
    </td>
</tr>